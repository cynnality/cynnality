## created: monday june 8 

for primarily css and js pages for reusable components/elements to be injected across other pages... 

starting with a player viewer -> implementing the shared player setup into that viewer -> 
so player "receipts" can be updated and edited from one source 
and the receipts will populate the dedicated player viewer page and will be injected onto other pages (like the wnba-season-viewer -> making the player names "buttons" to trigger their player receipt "popup)